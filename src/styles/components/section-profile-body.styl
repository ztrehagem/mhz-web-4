.section-profile-body {
  & {
    padding: 1rem 0;
  }

  > * {
    margin: 1rem auto;
    max-width: $base_width;
    width: 90%;
  }

  > p {
    margin-top: 3rem;
  }

  > .horizontal-list {
    margin-bottom: 3rem;
  }
}
