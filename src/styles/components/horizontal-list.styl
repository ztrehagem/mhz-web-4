.horizontal-list {
  & {
    display: flex;
    flex-wrap: wrap;
  }

  &.-left {
    justify-content: flex-start;
  }

  &.-center {
    justify-content: center;
  }

  &.-right {
    justify-content: flex-end;
  }

  > * {
    margin: 0.3rem;
  }
}
